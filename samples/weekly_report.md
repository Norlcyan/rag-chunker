# 项目周报 2026-04-30

本周进展汇总。

## 完成事项

### Markdown 切分器 MVP

完成结构感知切分的核心流水线：解析 → 切块 → 导出。支持 `#` / `##` / `###` 三级标题。

### 测试覆盖

新增 29 条测试用例，覆盖解析器边界、分块器行为、CLI 交互。

## 进行中

### PDF 文档支持

调研 PDF 解析库选型，需同时提取文本和结构信息。

## 待开始

### 图片描述生成

在 chunk 中附带图片的多模态描述，解决"图表含关键信息但不可被文本检索"的问题。

### 语义切分层

对无结构的文档，通过 embedding 相似度判定切分边界。需要评估 embedding API 的调用成本。

## 风险

以下两项需要提前关注：

| 风险项 | 等级 | 负责人 |
|--------|------|--------|
| PDF 页眉页脚干扰 | 中 | TBD |
| 大文件内存溢出 | 高 | TBD |

## 备注

以下内容来自 Slack 讨论：
> 应该优先保证 Markdown 路径的稳定性，PDF 可以等 Markdown 在生产环境跑两周再排期。
